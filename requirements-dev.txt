# Development dependencies for GenPy-Mini Phase 1.
#
# Runtime dependencies live in pyproject.toml under [project.dependencies].
# This file exists for tooling/CI that prefers a plain requirements file;
# prefer `pip install -e ".[dev]"` for local development.
-e .[dev]
