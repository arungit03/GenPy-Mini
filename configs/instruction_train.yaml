run:
  name: instruction-train-placeholder
  seed: 1337
  phase: future
model_config: configs/model/genpy_5m.yaml
data:
  instruction_dir: data/instruction
  evaluation_dir: data/evaluation
training:
  platform: kaggle_gpu
  batch_size: null
  learning_rate: null
  max_steps: null
  notes: placeholder only; no instruction tuning is performed in Phase 1
