# Sample Source Fixture

This directory is a small, fake "source repository" used only by
GenPy-Mini's Phase 2 automated tests (`tests/data/`). It is not a real
dataset source and is not registered in `config/dataset_sources.yaml`.
